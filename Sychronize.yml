---


  - name: "transfering file from one server to another"
    hosts: [fellowship]
    become: yes #execute as root
    tasks:

     - name: "moving one file from one server to another"
       synchronize:
         src: /home/vagrant/test
         dest: /home/vagrant/end
       delegate_to: gandalf

